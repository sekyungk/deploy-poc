name: Staging Deployment

on:
  push:
    branches:
      - master

jobs:
  d:
    name: Deploy Staging
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: build
        run: npm install && npm run build

      - name: TEST
        run: echo ${{ github.event }}

      - name: TEST
        run: echo ${{ secrets.staging.AWS_ACCESS_KEY }}
